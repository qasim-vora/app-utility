class ListTileItem {
  String title, subTitle;
  bool selected, uploaded;
  ListTileItem(
      {required this.title,
      required this.selected,
      required this.subTitle,
      required this.uploaded});
}
